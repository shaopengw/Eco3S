# 人口插件元数据
name: population
version: 1.0.0
description: 负责初始化人口规模、按出生率等规则更新总人口，并提供人口状态查询。
author: AgentWorld Team
enabled: true

# 插件类信息
plugin_class: DefaultPopulationPlugin
module: population_plugin

# 实现的接口
interfaces:
  - IPopulationPlugin
  - IPopulation

# 依赖项
dependencies:
  - map
  - towns

# 初始化参数
init_params:
  initial_population: 1000

# 配置项
config:
  enable_demographics: true
  enable_birth_death: false
