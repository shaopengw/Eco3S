# 居民插件元数据
name: residents
version: 1.0.0
description: 默认居民系统插件
author: AgentWorld Team
enabled: true

# 插件类信息
plugin_class: DefaultResidentsPlugin
module: resident_plugin

# 实现的接口
interfaces:
  - IResidentsPlugin

# 依赖项
dependencies:
  - map

# 初始化参数（可被 config/plugins.yaml 覆盖）
init_params:
  window_size: 3

config: {}
